Base surface for everything boxed. Use `href`/`onPress` to make it an accessible clickable card.

```jsx
<M3Card>…</M3Card>
<M3Card padding="sm" flat>…</M3Card>
<M3Card href="/indicators/demographics">…</M3Card>
```
Padding: `md · sm · none`. `flat` removes the shadow for nested cards.
