Anchors a section with a title and a right-aligned action (e.g. "Ver como tabela").

```jsx
<M3SectionHeader title="Pirâmide etária"
  action={<M3Button variant="text" icon="table">Ver como tabela</M3Button>} />
```
Props: `title`, `description`, `action` (JSX), `as` (heading level).
