Permanent banner for an LGPD-anonymized record — render at the top of the record and disable all edit actions while present.

```jsx
{patient.anonymized && <LgpdAnonymizedBanner />}
```
Not dismissible (compliance). PII shows as "—" elsewhere.
