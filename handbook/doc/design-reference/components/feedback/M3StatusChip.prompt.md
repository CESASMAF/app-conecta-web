Patient status pill — maps the lifecycle enum to color + icon + PT-BR label.

```jsx
<M3StatusChip status="active" />      {/* Acolhido */}
<M3StatusChip status="waitlisted" /> {/* Fila de espera */}
```
`waitlisted · active · discharged · withdrawn`. Use in the patient table, record header, and timeline.
